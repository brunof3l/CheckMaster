rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /checklists/{id}/{allPaths=**} {
      allow read, write: if request.auth != null; // refine por tenant/owner conforme necessário
    }
  }
}